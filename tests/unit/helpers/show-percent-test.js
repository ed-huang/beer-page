import { showPercent } from 'beer-page/helpers/show-percent';
import { module, test } from 'qunit';

module('Unit | Helper | show percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showPercent([42]);
  assert.ok(result);
});
